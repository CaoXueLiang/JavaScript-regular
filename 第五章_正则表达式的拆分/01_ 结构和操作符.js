// JavaScript 正则表达式中，都有哪些结构呢❓
// 字符字面量、字符组、量词、锚、分组、选择分支、反向引用。

// 操作符优先级
// 转义符(\) > 括号和方括号 > 量词({m,n} * ? +) > 位置和序列(^ $ \b \s 一般字符) > 管道符（|）

// ❗注意：
// 这里稍微总结一下，竖杠的优先级最低，即最后运算
// 另外关于元字符转义问题，当自己不确定与否时，尽管去转义，总之是不会错的。
